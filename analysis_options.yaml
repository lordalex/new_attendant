# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Suppress low-priority info messages
    avoid_print: ignore
    use_build_context_synchronously: ignore
    depend_on_referenced_packages: ignore
    unnecessary_getters_setters: ignore
    library_private_types_in_public_api: ignore
    # Naming conventions (FlutterFlow legacy + intentional for serialization)
    constant_identifier_names: ignore
    non_constant_identifier_names: ignore
    camel_case_types: ignore
    # Deprecated APIs (intentional usage)
    deprecated_member_use: ignore
    deprecated_member_use_from_same_package: ignore
    # Code patterns
    avoid_function_literals_in_foreach_calls: ignore
    type_literal_in_constant_pattern: ignore

linter:
  rules:
    # Disabled rules (FlutterFlow legacy code generates these)
    unnecessary_string_escapes: false
    prefer_const_constructors: false
    prefer_const_literals_to_create_immutables: false
    prefer_const_declarations: false

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options